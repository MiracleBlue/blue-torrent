import superComputed from '../../../utils/super-computed';
import { module, test } from 'qunit';

module('Unit | Utility | super computed');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = superComputed();
  assert.ok(result);
});
