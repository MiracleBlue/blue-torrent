import filesize from '../../../utils/filesize';
import { module, test } from 'qunit';

module('Unit | Utility | filesize');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = filesize();
  assert.ok(result);
});
